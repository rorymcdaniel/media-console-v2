---
phase: 01-foundation-and-build-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/IAudioOutput.h
  - src/platform/ICdDrive.h
  - src/platform/IGpioMonitor.h
  - src/platform/IDisplayControl.h
  - src/platform/PlatformFactory.h
  - src/platform/PlatformFactory.cpp
  - src/platform/stubs/StubAudioOutput.h
  - src/platform/stubs/StubAudioOutput.cpp
  - src/platform/stubs/StubCdDrive.h
  - src/platform/stubs/StubCdDrive.cpp
  - src/platform/stubs/StubGpioMonitor.h
  - src/platform/stubs/StubGpioMonitor.cpp
  - src/platform/stubs/StubDisplayControl.h
  - src/platform/stubs/StubDisplayControl.cpp
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "Four platform abstraction interfaces define the hardware contract without platform-specific code"
    - "Stub implementations exist for all four interfaces and provide simulated behavior for macOS development"
    - "PlatformFactory creates stub implementations on all platforms (real implementations added in later phases)"
    - "No #ifdef __linux__ guards exist anywhere in the codebase"
  artifacts:
    - path: "src/platform/IAudioOutput.h"
      provides: "Audio output interface: open, write, close, isOpen"
    - path: "src/platform/ICdDrive.h"
      provides: "CD drive interface: readToc, getDiscId, eject"
    - path: "src/platform/IGpioMonitor.h"
      provides: "GPIO monitor interface: start, stop, signals for encoder/switch events"
    - path: "src/platform/IDisplayControl.h"
      provides: "Display control interface: setPower, setBrightness, autoDetect"
    - path: "src/platform/PlatformFactory.h"
      provides: "Factory that creates platform implementations"
      contains: "createAudioOutput"
    - path: "src/platform/PlatformFactory.cpp"
      provides: "Runtime factory implementation using QSysInfo"
      contains: "StubAudioOutput"
    - path: "src/platform/stubs/StubAudioOutput.h"
      provides: "Stub audio output that accepts frames silently"
    - path: "src/platform/stubs/StubCdDrive.h"
      provides: "Stub CD drive that returns empty TOC"
    - path: "src/platform/stubs/StubGpioMonitor.h"
      provides: "Stub GPIO monitor with programmable events"
    - path: "src/platform/stubs/StubDisplayControl.h"
      provides: "Stub display control that logs commands"
  key_links:
    - from: "src/platform/PlatformFactory.cpp"
      to: "src/platform/stubs/StubAudioOutput.h"
      via: "std::make_unique<StubAudioOutput>()"
      pattern: "make_unique.*StubAudioOutput"
    - from: "src/platform/PlatformFactory.cpp"
      to: "src/platform/IAudioOutput.h"
      via: "returns unique_ptr<IAudioOutput>"
      pattern: "unique_ptr.*IAudioOutput"
---

<objective>
Define four platform abstraction interfaces (IAudioOutput, ICdDrive, IGpioMonitor, IDisplayControl) with stub implementations and a PlatformFactory that creates them at runtime.

Purpose: This replaces the original project's #ifdef __linux__ compile-time guards with runtime injection, enabling full macOS development with simulated hardware. Every subsequent hardware-dependent phase implements against these interfaces.
Output: Complete platform abstraction layer with stubs robust enough for real development.
</objective>

<execution_context>
@/Users/rory/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-build-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-build-infrastructure/01-RESEARCH.md

Reference original implementations to derive interface method signatures:
@~/Code/media-console/src/audio/AlsaOutput.h
@~/Code/media-console/src/disc/CdDrive.h
@~/Code/media-console/src/utils/VolumeEncoderMonitor.h
@~/Code/media-console/src/utils/InputEncoderMonitor.h
@~/Code/media-console/src/utils/ReedSwitchMonitor.h
@~/Code/media-console/src/utils/DisplayControl.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define four platform abstraction interfaces</name>
  <files>src/platform/IAudioOutput.h, src/platform/ICdDrive.h, src/platform/IGpioMonitor.h, src/platform/IDisplayControl.h</files>
  <action>
Study the original implementation headers listed in context to derive accurate method signatures. Each interface should capture what the original code actually does, not a guess at the API.

Create src/platform/IAudioOutput.h (study ~/Code/media-console/src/audio/AlsaOutput.h):
- #pragma once
- Pure virtual class (no QObject -- interfaces stay lightweight)
- virtual ~IAudioOutput() = default
- virtual bool open(const QString& deviceName, int sampleRate, int channels, int bitDepth) = 0
- virtual qint64 write(const char* data, qint64 frames) = 0 -- returns frames written
- virtual void close() = 0
- virtual bool isOpen() const = 0
- virtual QString deviceName() const = 0

Create src/platform/ICdDrive.h (study ~/Code/media-console/src/disc/CdDrive.h):
- #pragma once
- Pure virtual class
- virtual ~ICdDrive() = default
- Struct TocEntry { int trackNumber; int startSector; int endSector; int durationSeconds; }
- virtual QVector<TocEntry> readToc() = 0
- virtual QString getDiscId() = 0
- virtual bool eject() = 0
- virtual bool isDiscPresent() const = 0

Create src/platform/IGpioMonitor.h (study VolumeEncoderMonitor.h, InputEncoderMonitor.h, ReedSwitchMonitor.h):
- #pragma once
- #include QObject
- This IS a QObject because it needs signals
- Abstract QObject class with Q_OBJECT macro
- Signals: volumeUp(), volumeDown(), muteToggled(), inputNext(), inputPrevious(), inputSelect(), reedSwitchChanged(bool magnetsApart)
- virtual bool start() = 0
- virtual void stop() = 0
- NOTE: Single interface covering all three monitors. PlatformFactory creates one IGpioMonitor that handles all GPIO. The original had three separate classes but they all share the same lifecycle (start on init, stop on shutdown) and the same platform constraint (Linux only). Consolidating simplifies the factory and AppBuilder.

Create src/platform/IDisplayControl.h (study ~/Code/media-console/src/utils/DisplayControl.h):
- #pragma once
- #include QObject
- Abstract QObject class with Q_OBJECT macro (needs signals)
- Signals: displayDetected(int busNumber), powerChanged(bool on), brightnessChanged(int percent)
- virtual bool autoDetectDisplay() = 0
- virtual bool setPower(bool on) = 0
- virtual bool setBrightness(int percent) = 0
- virtual int brightness() const = 0
- virtual bool isPowered() const = 0
  </action>
  <verify>
    <automated>ls src/platform/IAudioOutput.h src/platform/ICdDrive.h src/platform/IGpioMonitor.h src/platform/IDisplayControl.h && echo "All interfaces exist"</automated>
  </verify>
  <done>Four interface headers exist with method signatures derived from original implementation code, using pure virtual methods and no platform-specific includes</done>
</task>

<task type="auto">
  <name>Task 2: Create stub implementations and PlatformFactory</name>
  <files>src/platform/stubs/StubAudioOutput.h, src/platform/stubs/StubAudioOutput.cpp, src/platform/stubs/StubCdDrive.h, src/platform/stubs/StubCdDrive.cpp, src/platform/stubs/StubGpioMonitor.h, src/platform/stubs/StubGpioMonitor.cpp, src/platform/stubs/StubDisplayControl.h, src/platform/stubs/StubDisplayControl.cpp, src/platform/PlatformFactory.h, src/platform/PlatformFactory.cpp</files>
  <action>
Per CONTEXT.md locked decision: "Stubs must be robust enough for real development, not just compilation" and "stub ALSA output that accepts frames silently, stub GPIO that can be driven programmatically, stub display control that logs commands."

Create StubAudioOutput (implements IAudioOutput):
- open(): stores parameters, sets m_open = true, logs via qCInfo(mediaAudio), returns true
- write(): accepts frames silently (no-op), returns frames count as if written
- close(): sets m_open = false, logs
- isOpen(): returns m_open
- deviceName(): returns "stub:null"

Create StubCdDrive (implements ICdDrive):
- readToc(): returns empty QVector (no disc in stub)
- getDiscId(): returns empty string
- eject(): logs, returns true
- isDiscPresent(): returns false (can be toggled programmatically for testing via setDiscPresent(bool))

Create StubGpioMonitor (implements IGpioMonitor):
- start(): logs "Stub GPIO monitor started", returns true
- stop(): logs "Stub GPIO monitor stopped"
- Add public methods for programmatic driving in tests:
  - simulateVolumeUp() -- emits volumeUp()
  - simulateVolumeDown() -- emits volumeDown()
  - simulateMuteToggle() -- emits muteToggled()
  - simulateInputNext() -- emits inputNext()
  - simulateInputPrevious() -- emits inputPrevious()
  - simulateInputSelect() -- emits inputSelect()
  - simulateReedSwitch(bool apart) -- emits reedSwitchChanged(apart)

Create StubDisplayControl (implements IDisplayControl):
- autoDetectDisplay(): logs "Stub display detected", emits displayDetected(1), returns true
- setPower(bool on): logs command, stores state, emits powerChanged(on), returns true
- setBrightness(int pct): logs command, stores state, emits brightnessChanged(pct), returns true
- brightness(): returns m_brightness (default 100)
- isPowered(): returns m_powered (default true)

Create PlatformFactory:
- Static methods returning std::unique_ptr for each interface
- static std::unique_ptr<IAudioOutput> createAudioOutput()
- static std::unique_ptr<ICdDrive> createCdDrive()
- static std::unique_ptr<IGpioMonitor> createGpioMonitor(QObject* parent = nullptr) -- QObject needs parent
- static std::unique_ptr<IDisplayControl> createDisplayControl(QObject* parent = nullptr)
- For Phase 1: ALL factory methods return stub implementations unconditionally
- Include a comment: "// TODO: Phase 4+ add runtime detection: if (QSysInfo::kernelType() == 'linux') return real impl"
- Include static bool isLinux() helper method using QSysInfo::kernelType()

Update CMakeLists.txt to add all new source files to qt_add_executable. Add all .h and .cpp files from src/platform/ and src/platform/stubs/.
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -20 && echo "Build: $?"</automated>
  </verify>
  <done>All four stub implementations compile, PlatformFactory creates them successfully, project builds clean with no platform-specific guards</done>
</task>

</tasks>

<verification>
- [ ] All four interface headers exist with pure virtual methods
- [ ] All four stub implementations compile and link
- [ ] PlatformFactory creates all four stubs successfully
- [ ] No #ifdef __linux__ or platform-specific guards exist in any file
- [ ] StubGpioMonitor has simulate* methods for programmatic testing
- [ ] StubDisplayControl logs commands to qCInfo(mediaApp)
- [ ] StubAudioOutput accepts frames silently
- [ ] Project builds clean after adding all platform files
</verification>

<success_criteria>
Platform abstraction layer is complete with four interfaces, four stub implementations, and a PlatformFactory. The stubs are robust enough for macOS development (simulated behavior, not just compilation stubs). No compile-time platform guards exist.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-build-infrastructure/01-02-SUMMARY.md`
</output>
