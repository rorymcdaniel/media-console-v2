---
phase: 01-foundation-and-build-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - src/app/AppConfig.h
  - src/app/AppConfig.cpp
  - src/app/AppContext.h
  - src/app/AppBuilder.h
  - src/app/AppBuilder.cpp
  - src/main.cpp
  - tests/CMakeLists.txt
  - tests/test_AppConfig.cpp
  - tests/test_AppBuilder.cpp
  - tests/test_PlatformFactory.cpp
  - tests/test_StubAudioOutput.cpp
  - tests/test_StubCdDrive.cpp
  - tests/test_StubGpioMonitor.cpp
  - tests/test_StubDisplayControl.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - FOUND-04
  - FOUND-07
  - FOUND-08

must_haves:
  truths:
    - "AppConfig loads all settings from QSettings INI file in one call and provides typed access via nested structs"
    - "AppBuilder constructs the full object graph and returns AppContext with all platform stubs wired"
    - "Google Test suite executes via CTest with at least one test per platform interface using stub implementations"
    - "No code outside AppConfig.cpp touches QSettings directly"
    - "main.cpp delegates all construction to AppBuilder and is under 30 lines"
  artifacts:
    - path: "src/app/AppConfig.h"
      provides: "Typed configuration struct with nested subsystem configs"
      contains: "struct AppConfig"
    - path: "src/app/AppConfig.cpp"
      provides: "One-time QSettings load"
      contains: "QSettings"
    - path: "src/app/AppContext.h"
      provides: "Struct holding all constructed objects"
      contains: "struct AppContext"
    - path: "src/app/AppBuilder.h"
      provides: "Composition root class"
      contains: "class AppBuilder"
    - path: "src/app/AppBuilder.cpp"
      provides: "Object graph construction"
      contains: "AppContext"
    - path: "tests/CMakeLists.txt"
      provides: "Google Test integration via FetchContent"
      contains: "gtest_discover_tests"
    - path: "tests/test_AppConfig.cpp"
      provides: "AppConfig loading tests"
      contains: "TEST"
    - path: "tests/test_AppBuilder.cpp"
      provides: "Composition root tests"
      contains: "TEST"
    - path: "tests/test_PlatformFactory.cpp"
      provides: "Factory creation tests"
      contains: "TEST"
    - path: "tests/test_StubAudioOutput.cpp"
      provides: "StubAudioOutput behavior tests"
    - path: "tests/test_StubCdDrive.cpp"
      provides: "StubCdDrive behavior tests"
    - path: "tests/test_StubGpioMonitor.cpp"
      provides: "StubGpioMonitor signal tests"
    - path: "tests/test_StubDisplayControl.cpp"
      provides: "StubDisplayControl behavior tests"
  key_links:
    - from: "src/app/AppBuilder.cpp"
      to: "src/platform/PlatformFactory.h"
      via: "PlatformFactory::createAudioOutput()"
      pattern: "PlatformFactory::create"
    - from: "src/app/AppBuilder.cpp"
      to: "src/app/AppConfig.h"
      via: "build(const AppConfig&)"
      pattern: "AppConfig"
    - from: "src/app/AppBuilder.cpp"
      to: "src/app/AppContext.h"
      via: "returns AppContext"
      pattern: "AppContext"
    - from: "src/main.cpp"
      to: "src/app/AppBuilder.h"
      via: "AppBuilder().build(config)"
      pattern: "AppBuilder"
    - from: "tests/CMakeLists.txt"
      to: "CMakeLists.txt"
      via: "add_subdirectory(tests)"
      pattern: "add_subdirectory.*tests"
---

<objective>
Create the typed configuration system (AppConfig), composition root (AppBuilder + AppContext), Google Test framework integration, and comprehensive test suite covering all platform stubs.

Purpose: AppConfig eliminates scattered QSettings reads. AppBuilder replaces the 325-line main.cpp with structured object construction. Tests validate the entire foundation layer. Together, these complete the project skeleton.
Output: Typed config, composition root, and passing test suite for all Phase 1 components.
</objective>

<execution_context>
@/Users/rory/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rory/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-build-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-build-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-and-build-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-and-build-infrastructure/01-02-SUMMARY.md

Reference original for configuration values:
@~/Code/media-console/src/main.cpp

<interfaces>
From Plan 01 artifacts:
- src/utils/Logging.h: void initLogging(const QString& filterRules)

From Plan 02 artifacts:
- src/platform/IAudioOutput.h: open, write, close, isOpen, deviceName
- src/platform/ICdDrive.h: readToc, getDiscId, eject, isDiscPresent, TocEntry struct
- src/platform/IGpioMonitor.h: QObject with start/stop and signals (volumeUp, volumeDown, muteToggled, inputNext, inputPrevious, inputSelect, reedSwitchChanged)
- src/platform/IDisplayControl.h: QObject with autoDetectDisplay, setPower, setBrightness, brightness, isPowered and signals
- src/platform/PlatformFactory.h: static createAudioOutput, createCdDrive, createGpioMonitor, createDisplayControl
- src/platform/stubs/StubGpioMonitor.h: simulate* methods for programmatic testing
- src/platform/stubs/StubCdDrive.h: setDiscPresent(bool) for testing
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create AppConfig typed configuration and AppBuilder composition root</name>
  <files>src/app/AppConfig.h, src/app/AppConfig.cpp, src/app/AppContext.h, src/app/AppBuilder.h, src/app/AppBuilder.cpp, src/main.cpp, CMakeLists.txt</files>
  <behavior>
    - AppConfig::loadFromSettings() reads all values from QSettings INI and returns a populated struct
    - AppConfig default values match original main.cpp: receiver host "192.168.68.63", port 60128, display id 1, api port 8080, etc.
    - AppBuilder::build(config) constructs all platform objects via PlatformFactory and returns AppContext
    - AppContext contains non-null pointers to all four platform interface implementations
    - main.cpp uses AppBuilder and is under 30 lines
  </behavior>
  <action>
Create src/app/AppConfig.h with nested structs per QSettings group (per Claude's discretion area in CONTEXT.md -- choosing nested structs because it maps cleanly to INI groups and const ref passing):

Nested structs:
- ReceiverConfig: host ("192.168.68.63"), port (60128)
- DisplayConfig: id (1), dimTimeoutSeconds (300), offTimeoutSeconds (1200), dimBrightness (25), timeoutEnabled (true)
- CdConfig: devicePath ("/dev/cdrom"), pollIntervalMs (1000), audioOnly (true)
- LibraryConfig: rootPath ("/data/media/music/")
- ApiConfig: port (8080)
- LoggingConfig: filterRules (empty string = default info level)
- SpotifyConfig: clientId (empty), clientSecret (empty), desiredDeviceName ("Voice of Music")

AppConfig struct with static AppConfig loadFromSettings() method.
All default values lifted from original main.cpp lines 53-175.

Create src/app/AppConfig.cpp:
- AppConfig::loadFromSettings() implementation
- Creates QSettings (uses default org/app name set by main.cpp)
- Reads each group's values with settings.value("key", default)
- Returns populated AppConfig by value
- This is the ONLY file in the project that includes QSettings

Create src/app/AppContext.h:
- struct AppContext with non-owning pointers to IAudioOutput*, ICdDrive*, IGpioMonitor*, IDisplayControl*
- All default to nullptr
- Future phases add: receiverState, playbackState, uiState, controllers, etc.

Create src/app/AppBuilder.h and AppBuilder.cpp:
- class AppBuilder : public QObject
- AppContext build(const AppConfig& config)
- build() creates all four platform implementations via PlatformFactory
- Sets QObject parent to AppBuilder for lifetime management
- Calls initLogging(config.logging.filterRules)
- Returns filled AppContext struct
- Ownership: AppBuilder owns all created objects (QObject parent chain). AppContext holds non-owning pointers.

Update src/main.cpp to use AppBuilder:
- Load AppConfig via AppConfig::loadFromSettings()
- Create AppBuilder on stack (parented to app)
- Call build(config) to get AppContext
- Create QQmlApplicationEngine
- Load QML
- Return app.exec()
- Target: under 30 lines of code in main()

Restructure CMakeLists.txt:
- Create add_library(media-console-lib STATIC ...) with all source files EXCEPT main.cpp
- qt_add_executable(media-console src/main.cpp) links against media-console-lib
- Test target also links against media-console-lib
- This avoids duplicate compilation and enables testing without main()

Add all new src/app/ files to the library target.
  </action>
  <verify>
    <automated>cmake --build build 2>&1 | tail -20 && echo "Build result: $?"</automated>
  </verify>
  <done>AppConfig loads all settings via static method, AppBuilder constructs object graph and returns AppContext, main.cpp delegates to AppBuilder and is under 30 lines, project compiles clean</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Google Test suite with tests for all Phase 1 components</name>
  <files>tests/CMakeLists.txt, tests/test_AppConfig.cpp, tests/test_AppBuilder.cpp, tests/test_PlatformFactory.cpp, tests/test_StubAudioOutput.cpp, tests/test_StubCdDrive.cpp, tests/test_StubGpioMonitor.cpp, tests/test_StubDisplayControl.cpp</files>
  <behavior>
    - PlatformFactory creates non-null instances of all four interfaces
    - StubAudioOutput: open succeeds, write returns frame count, close works, deviceName returns "stub:null"
    - StubCdDrive: readToc returns empty, getDiscId returns empty, isDiscPresent returns false, eject returns true
    - StubGpioMonitor: start/stop succeed, simulate* methods emit correct signals (verify with QSignalSpy)
    - StubDisplayControl: setPower/setBrightness store state and emit signals, defaults are powered=true brightness=100
    - AppConfig: default values match expected (receiver host, port, etc.)
    - AppBuilder: build() returns AppContext with all non-null pointers
  </behavior>
  <action>
Create tests/CMakeLists.txt:
- include(FetchContent)
- FetchContent_Declare googletest from https://github.com/google/googletest.git tag v1.17.0
- FetchContent_MakeAvailable(googletest)
- add_executable(media-console-tests ...) listing all test .cpp files
- target_link_libraries: GTest::gtest_main, Qt6::Core, Qt6::Test (for QSignalSpy), media-console-lib
- target_include_directories pointing to the project src directory
- include(GoogleTest) and gtest_discover_tests(media-console-tests)

Create tests/test_PlatformFactory.cpp:
- TEST(PlatformFactory, CreatesNonNullAudioOutput) -- createAudioOutput() returns non-null
- TEST(PlatformFactory, CreatesNonNullCdDrive) -- createCdDrive() returns non-null
- TEST(PlatformFactory, CreatesNonNullGpioMonitor) -- createGpioMonitor() returns non-null
- TEST(PlatformFactory, CreatesNonNullDisplayControl) -- createDisplayControl() returns non-null

Create tests/test_StubAudioOutput.cpp:
- TEST(StubAudioOutput, OpenSucceeds) -- open with valid params returns true
- TEST(StubAudioOutput, WriteReturnsFrameCount) -- write returns correct frame count
- TEST(StubAudioOutput, CloseWorks) -- close sets isOpen to false
- TEST(StubAudioOutput, DeviceNameIsStub) -- deviceName returns "stub:null"

Create tests/test_StubCdDrive.cpp:
- TEST(StubCdDrive, ReadTocReturnsEmpty) -- no disc
- TEST(StubCdDrive, GetDiscIdReturnsEmpty) -- no disc
- TEST(StubCdDrive, IsDiscPresentDefaultsFalse)
- TEST(StubCdDrive, EjectReturnsTrue)

Create tests/test_StubGpioMonitor.cpp:
- TEST(StubGpioMonitor, StartSucceeds)
- TEST(StubGpioMonitor, SimulateVolumeUpEmitsSignal) -- use QSignalSpy to verify signal emission
- TEST(StubGpioMonitor, SimulateReedSwitchEmitsSignal) -- verify bool parameter passed correctly

Create tests/test_StubDisplayControl.cpp:
- TEST(StubDisplayControl, DefaultsPoweredAndFullBrightness) -- isPowered true, brightness 100
- TEST(StubDisplayControl, SetPowerStoresState) -- setPower(false) then isPowered() returns false
- TEST(StubDisplayControl, SetBrightnessStoresAndEmits) -- verify with QSignalSpy
- TEST(StubDisplayControl, AutoDetectSucceeds) -- returns true

Create tests/test_AppConfig.cpp:
- TEST(AppConfig, DefaultReceiverHost) -- verify "192.168.68.63"
- TEST(AppConfig, DefaultReceiverPort) -- verify 60128
- TEST(AppConfig, DefaultApiPort) -- verify 8080
- TEST(AppConfig, DefaultDisplayTimeout) -- verify 300s dim, 1200s off
- These test default struct values (no QSettings needed for default tests)

Create tests/test_AppBuilder.cpp:
- TEST(AppBuilder, BuildReturnsNonNullContext) -- all pointers non-null
- TEST(AppBuilder, ContextHasAudioOutput) -- audioOutput != nullptr
- TEST(AppBuilder, ContextHasCdDrive) -- cdDrive != nullptr
- TEST(AppBuilder, ContextHasGpioMonitor) -- gpioMonitor != nullptr
- TEST(AppBuilder, ContextHasDisplayControl) -- displayControl != nullptr
- Each test creates AppConfig with defaults, calls AppBuilder::build(), checks AppContext fields

Ensure CMakeLists.txt top-level has add_subdirectory(tests) inside the BUILD_TESTS conditional.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build --output-on-failure 2>&1 | tail -30</automated>
  </verify>
  <done>All tests pass via CTest, at least one test per platform interface stub, AppConfig defaults verified, AppBuilder produces valid AppContext, gtest_discover_tests finds all tests automatically</done>
</task>

</tasks>

<verification>
- [ ] `cmake --build build` compiles all source and test code
- [ ] `ctest --test-dir build --output-on-failure` runs all tests and they pass
- [ ] AppConfig.h has nested structs for each subsystem
- [ ] AppConfig.cpp is the ONLY file importing QSettings
- [ ] AppBuilder.build() returns AppContext with all four platform pointers non-null
- [ ] main.cpp is under 30 lines and delegates to AppBuilder
- [ ] Google Test fetched via FetchContent at tag v1.17.0
- [ ] gtest_discover_tests(media-console-tests) in tests/CMakeLists.txt
- [ ] At least one test per stub implementation validates behavior (not just existence)
- [ ] media-console-lib static library target exists for test linking
</verification>

<success_criteria>
AppConfig provides typed access to all configuration without scattered QSettings reads. AppBuilder constructs the complete object graph and returns AppContext. All Google Tests pass via CTest, covering every platform stub interface and the composition root. main.cpp is clean and minimal.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-build-infrastructure/01-03-SUMMARY.md`
</output>
